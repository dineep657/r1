name: Keep Render Alive
on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl health endpoint
        run: |
          curl -fsS https://realtime-code-backend-upxc.onrender.com/api/health > /dev/null || true


